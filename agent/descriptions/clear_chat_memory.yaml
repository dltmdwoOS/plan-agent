args:
  message:
    title: Message
    type: string
description: "This tool is a special tool. Use it when you want to delete or reset\
  \ the conversation history with the user so far.\n\nArgs:\n    message (str): The\
  \ user's question or request that triggered this tool (e.g., 'Delete the memory.',\
  \ 'Reset the conversation history so far.')"
kind: special
name: clear_chat_memory
